const userPool = [
    {
        username: "hoagieman5000",
        badges: [{ type: "moderator" }]
    },
    {
        username: "hoagiebot5000",
        badges: [{ type: "vip" }]
    },
    {
        username: "twitchuser123",
    },
]

/*
const intervalHandle = setInterval(() => {
    console.log("Generating chat message event...")
    const randUser = userPool[Math.floor(Math.random() * userPool.length)]
    generateChatMessage(randUser, "hi hi hi hi hi")
}, 1000)

setTimeout(() => clearInterval(intervalHandle), 10000)
*/
